# Cilium

--8<-- "_shared/toolsets/cilium.md"

## Holmes CLI

Before enabling the toolsets, ensure your tools are properly configured:

```bash
# Verify Cilium is accessible
cilium version
cilium status

# Verify Hubble is accessible (if using Hubble tools)
hubble version
hubble status
```
